<% content_for :body do %>
  <ul id="tenants">
    <% @tenants.each do |tenant| %>
      <li>
        <%= link_to tenant.name, refinery.tenants_tenant_path(tenant) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
